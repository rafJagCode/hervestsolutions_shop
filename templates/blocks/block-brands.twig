{#
// inputs:
// - layout: string - one of [columns-8-full, columns-7-sidebar] (default: columns-8-full)
// - producers: lista producentów
#}

{% set layout = inputs.layout|default('columns-8-full') %}
{% set root_classes = classnames('block block-brands block-brands--layout--'~layout) %}

{% set brands = data('block-brands.json') %}
{% set producers = inputs.producers %}

<div class="{{ root_classes }}">
    <div class="container">
        <ul class="block-brands__list">
            {% for producer in producers %}
                <li class="block-brands__item">
                    <a href="/price-list" class="block-brands__item-link">
                        <img src="{{ image(producer.image) }}" alt="">

                        <span class="block-brands__item-name">{{ producer.Name }}</span>
                    </a>
                </li>
                <li class="block-brands__divider" role="presentation"></li>
            {% endfor %}
        </ul>
    </div>
</div>
